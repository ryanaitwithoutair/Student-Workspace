/** Minimum background visibility — dimming slider at or below this keeps base visibility */
export const MIN_BG_VISIBILITY = 0.75;

export const DEFAULT_OVERLAY_OPACITY = 0.75;

export const TIMER_DURATIONS = {
  pomodoro: 25,
  shortBreak: 5,
  longBreak: 15,
};

export const MAX_CUSTOM_MINUTES = 360;
export const MIN_FOCUS_MINUTES = 1;
